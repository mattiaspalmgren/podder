import React from 'react';

const Header = (props) => {
  const {  } = props;
  
  return (
    <div className="grid">
      <div className="col col-12">
        <h1>Podder</h1>
      </div>
    </div>
  );
};

export default Header;